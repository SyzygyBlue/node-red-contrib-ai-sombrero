/**
 * Prompt Module
 * Exports all prompt-related functionality
 */

const { buildPrompt, validatePrompt } = require('./prompt-builder');

module.exports = {
  buildPrompt,
  validatePrompt,
  // Additional prompt-related exports can be added here
};
